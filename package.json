{
  "name": "agentalk-mcp-client",
  "version": "1.0.0",
  "description": "AgentTalk MCP attach-mode worker (harness + llm-agent + pty drivers)",
  "bin": {
    "llm-agent": "./llm-agent.mjs",
    "agent-launcher": "./agent-launcher.mjs"
  },
  "scripts": {
    "lint": "eslint .",
    "launcher": "node agent-launcher.mjs",
    "test": "vitest run",
    "verify-contract": "node scripts/verify-contract.js",
    "sync-contract": "node scripts/sync-wire-contract.js",
    "build": "npm run lint && npm run verify-contract && npm run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "node-pty": "^1.1.0",
    "strip-ansi": "^7.2.0",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "eslint": "^10.5.0",
    "vitest": "^4.1.9"
  }
}
